# 第一部分：二维图像与视觉表示

## 第一章：数字图像，从光线到一张可计算的数组

### 1.1 相机记录的是光，不直接记录物体

从 **”光度一致性假设“** 这个理论入手：什么是光度一致性？

多视图匹配的原理：算法通常需要找到两张照片中的”同一个点“，而算法通常假设：既然是同一个点，那么在两张照片里的颜色/亮度应当差不多。

这里我们来更本质的看清这件事情：

相机记录的是光，而非物体的本身，而光自然会受到以下内容的影响：

- 材质：物体表面会反射什么颜色的光（不同波长如何反射）
- 光源：照射物体的光什么颜色，有多强（入射光谱和强度）
- 几何位置：光线射入的角度
- 镜头：如何聚焦光线

那么由于受到这些因素的影响，我们就要时刻谨慎的对待 **光度一致性假设**

### 1.2 从辐射到像素：一条简化成像链

明白了相机记录的是光而非物体本身，那么进一步就需要考虑 **相机上的一个像素值是怎么来的**

本质上是经过了一条长长的成像链之后得到，而不是真实世界的直接复制：

- 物理光学阶段
	- **场景辐射** ： 现实世界的物体辐射或者反射光线
	- **镜头聚焦** ：相机手机光线并且进行聚焦
	- **曝光控制** ：光圈和快门决定光线进入的能量
- 光电转换阶段
	- **光子转电荷** ：光落到传感器上会转化成电荷，光线越强，电荷越多
	- 噪声引入：信号并非纯净，有各种噪声（热噪声，散粒噪声等）
	- 响应函数：传感器对光的反应不是完全线性，需要一个相应函数去描述
- 数字信号处理阶段
	- **量化** ：模拟电路将电荷信号转化成离散的数字信号。此时得到的数据通常称为RAW数据
	- **ISP处理** （ *Image Signal Processor* ，图像信号处理器）：原始RAW数据往往是灰暗，偏色且充满噪点的，需要经过ISP处理：
		- 去马赛克：传感器通常只记录红绿蓝中的一种颜色，需要通过算法得到确实的另外两种颜色
		- 白平衡：校正色温，让白色的物体在不同的灯光下依然是白色的
		- 颜色校正：调整色彩，使其符合人眼
		- 降噪：去除噪点
		- 色调映射：真实世界的亮度范围很大，但是屏幕显示的亮度范围有限，需要进行压缩和调整，让亮部不过曝，暗部有细节

> 这是简化版，后文随着知识的铺垫，会更细化和纠正，不必在此纠结

补充说明的内容：
1. 物理光学阶段，理想曝光量公式 $E∝LAT$
	- $E:$ 理想曝光量
	- $L:$ 场景的亮度，或者说到达镜头的辐亮度
	- $A:$ 光圈的开口面积
	- $T:$ 曝光时间，和快门有关
2. 光信号处理过程：像素值的公式 $p=Q(g(E+n))$
	- $p:$ 照片上的像素值
	- $E+n:$ 加入噪声
	- $g(...):$ 响应函数，光电转化的时候理论上应当是线性变化，但是受制于真实的物理硬件，传感器往往有非线性相应，函数g就描述了这样一种物理硬件的特性
	- $Q(...):$ 量化和ISP处理，将模拟电信号转化成离散的数字，当然中间经历了上文提到的很多处理

说了这么多，希望建立起一个直觉：最终照片上的RGB事实上已经经过大量的非线性处理，因此，即使是所谓的“RGB相同”，也不过是描述“可能是同一个物体”的近似说法，即，RGB相同并不代表是同一个物体。

### 1.3 曝光三角：光圈、快门和增益的影响并不相同

摄影里有个著名的“曝光三角”：光圈、快门、ISO（增益）。它们都能让照片变亮或变暗，但**它们带来的副作用是完全不同的**。

先补充一个概念：

景深（Depth of Field,DOF），简单来说就是：照片“清晰”的范围有多深
- 浅景深（背景虚化）：只有你对焦的主体清晰，它的前面和后面都糊成了一片（刀锐奶化），此时景深非常窄可能只有几厘米，目的是突出主体
- 景深深（全景清晰）：从近景到极远处的山，全部清晰，景深非常宽，目的是交代所有细节

然后我们具体来看曝光三角各自的影响：

1. **光圈变大 副作用：改变景深和衍射**
    - **好处**：进光孔大了，照片变亮了。
    - **坏处**：景深变浅了。这意味着，原本清晰可见的背景细节现在模糊了。对于需要提取背景特征点的几何算法来说，这是致命的，因为特征点消失了。此外，光圈变化还会引起光学上的衍射效应变化。
2. **快门变长  副作用：运动模糊**
    - **好处**：感光时间长了，照片变亮了。
    - **坏处**：如果相机在动，或者场景里有东西在动，画面就会糊成一片（拖影）。模糊的图像会让算法找不到准确的边缘和角点，导致匹配失败。
3. **增益 (ISO) 变大 副作用：放大噪声**
    - **好处**：强行把微弱的电信号放大，照片变亮了。
    - **坏处**：信号放大的同时，底噪也被放大了。画面会出现大量粗糙的颗粒感（噪点）。这些随机的噪点会被算法误认为是真实的纹理特征，产生大量的错误匹配。

说了这么多，只需要建立一个直觉：曝光的变化比较复杂，同时会给算法带来非常大的麻烦

进一步的，CV领域的各大门派，是怎么解决这个问题的呢：

- 传统派——多视图几何：
	“多视图几何通常更关心位置关系，因此可以通过归一化，特征学习或鲁棒损失来减弱曝光差异。”
	- 多视图几何：就是传统的三维重建，SLAM(及时定位于地图重建)，SfM（运动恢复结构）。这类算法的核心任务是：通过好几张图片，算出相机在空间中的位置，以及场景的三维点云
	- 三大操作：
		- 归一化相关（Normalized Correlation）：比较两张照片的同一块区域时，将各自平均亮度减去
		- 特征学习（Feature Learning）：利用深度学习训练出来的特征提取器，他们就有经过这方面的训练，可以自动忽略因为光照变化产生的明暗差异
		- 鲁棒损失（robust lost）：在数学优化中，需要不断的计算方差，如果统一的使用均方误差，一旦某张照片出现突然的过曝或者欠曝，就会产生一个恐怖的惩罚值，带偏整个优化过程，而鲁棒损失函数就会灵活的调整这个误差的计算方式：比如正常光照匹配的时候，用平方惩罚，误差极大的时候就将惩罚降级为线性，甚至直接忽略
	传统派本质上是跳过了曝光的问题，选择性的无视亮度差异，只抓住不变的结构
	
- 前沿派——（NeRF/高斯渲染）
	“神经辐射场和高斯渲染若追求照片级重建，还可能显式建模每张图的曝光或外观嵌入”
	- 神经辐射场（NeRF)和3D高斯泼溅(3DGS)，目标不是粗糙的点云，而是要渲染和原图一摸一样，连光影都一模一样的逼真画面，所以，曝光问题不能无视，必须解决
	- 操作：外观嵌入
		既然每张照片的曝光，白平衡都不一样，那么就给每一张照片一个独特的向量（embedding），之后再输入网络，让网络在训练的时候自己领悟各个照片的处理方式有所区别

### 1.4 颜色：RGB是一个坐标表示，物体并不存在固定的RGB数值


要理解这句话，首先我们先了解什么是RGB。

RGB最本质的含义就是：一个像素由红、绿、蓝三个子通道组成，每个通道有一个数值，代表该颜色的“强度”。

这个三维数字坐标通过某种方式映射到 **色彩空间** 中，对应一个特定的颜色。

如同温标一样，不同的255的标准色，会导致即使是同一个RGB数值，其对应到各自色彩空间中特定的颜色也是不同的。

在最常见的八位图片中，每个通道的范围是0-255，比如：
- （255，0，0）纯红
- （0，255，0）纯绿
- （0，0，255）纯蓝
- （255，255，255）纯白
- （0，0，0）纯黑

此外，关于我们提到的 **强度** 在不同的RGB中含义也不相同。

- Linear RGB：数值直接对应物理光能，强度和数值线性正比，一般给物理引擎和AI计算使用
- sRGB：数值经过 Gamma 编码，是为了人眼舒适而非线性处理过的，和真实的物理强度不成正比

既然如此，在这里提到Gamma编码，我们就呼应上文的内容，把从光进入传感器之后的过程具体化，方便理解这两种RGB的区别：

- 传感器端（ISP之前）
	1. 光电转换：
		光子打到感光二极管上，光子越多，电荷越多（光电效应）。此时信号还是模拟电压，及其微弱
	2. 模拟增益（Analog Gain）：
		对模拟信号进行放大，放大倍数越大画面越亮，但是噪声也被放大了。
	3. ADC(Analog-to-Digital Converter) 量化
		把放大后的模拟电压转化成数字值。比如 12-bit ADC量化，数值范围就是0-4095
	
	到这里，我们得到了什么：
		Bayer RAW 数据，此时此刻，每个像素都只有R，G，B中的一个颜色值，整体是马赛克状的，而不是完整的RGB图像

- ISP内部
	1. 黑电平校正（BLC）
		传感器即使没有光，也会因为暗电流产生一个不为零的基础信号。这一步把像素值减去这个底噪，让黑色归零
	2. 坏点校正（BPC/DPC）
		传感器上可能有坏死的像素（始终量或者始终暗），用周围正常像素值进行替换修复
	3. 镜头阴影校正（LSC）
		镜头边缘进光量比中心少，画面四角会偏暗（暗角）。这一步对不同位置的像素做亮度补偿
	4. 数字增益（Digital Gain）
		如果模拟增益不够亮，那么在这里再补一刀。模拟增益和数字增益合在一起就是常说的ISO
	5. 去马赛克（Demosaicing）
		通过插值算法，根据每个像素周围相邻像素的颜色信息，推算出该像素确实的另外两个颜色值，第一次得到RGB数值
	6. 白平衡（AWB）
		估算场景光源的色温，对RGB三个通道分别乘以不同的增益系数，使得白色物体还原为白色
	7. 色彩校正矩阵（CCM）
		传感器的滤光片响应和人眼/标准色彩空间不完全匹配，这时用一个 三乘三 的矩阵做线性变换，把颜色校准到目标色彩空间
	8. Gamma校正
		把线性光数据做非线性映射（大约1/2.2次幂），减少数据损失并，适配人眼感知和显示器特性
	9. 后处理与输出
		降噪，锐化，色调映射，色彩空间转换，压缩编码等，最终输出成品图像
	
	从Gamma校正开始，RGB被非线性方式修饰，在此之前都是线性数据（和物理光能可线性对应）

---

值得一提的是，在这里图片经过Gamma校正之后，存储，然而在显示器展示的时候还会做一步逆Gamma变化，将数据还原。那么问题来了，为什么还要Gamma校正呢？

CCM 之后，每个像素有一个线性光值，比如 R = 0.037。这个值的含义是：**物理光能是最大值的 3.7%**。

现在你要把它存成 8-bit 整数（0~255）。

直接存会怎样（不做 Gamma）

最简单的映射方式：`stored_value = round(linear_value × 255)`

我们来算几个暗部的值：

| 线性光值  | ×255  | 四舍五入后 |
| ----- | ----- | ----- |
| 0.001 | 0.255 | **0** |
| 0.002 | 0.51  | **1** |
| 0.003 | 0.765 | **1** |
| 0.004 | 1.02  | **1** |
| 0.005 | 1.275 | **1** |
| 0.006 | 1.53  | **2** |

看到了吗？

- 线性光值 0.002、0.003、0.004、0.005 这四个**完全不同的物理亮度**，存进去之后全都变成了 **1**。
- 它们之间的差异**永久丢失**了。
- 等以后要用的时候（比如 NeRF 训练、或者显示器显示），你只知道这个像素的值是 1，但你无法知道它原来是 0.002 还是 0.005。

这就是"信息丢失"——**不同的输入被量化成了同一个输出，且不可逆。**

做了 Gamma 再存会怎样

Gamma 编码：`encoded = linear_value ^ (1/2.2)`，然后再 ×255 取整。

同样的暗部值：

|线性光值|Gamma 编码后|×255|四舍五入后|
|---|---|---|---|
|0.001|0.046|11.7|**12**|
|0.002|0.079|20.1|**20**|
|0.003|0.105|26.8|**27**|
|0.004|0.128|32.6|**33**|
|0.005|0.149|38.0|**38**|
|0.006|0.169|43.1|**43**|
现在：

- 0.002 → 20
- 0.003 → 27
- 0.004 → 33
- 0.005 → 38

**每一个值都不同，全部保留了。**

为什么 Gamma 能做到这一点

因为 `x^(1/2.2)` 这个函数在 x 接近 0 的时候，**斜率非常大**。

- 线性空间里，0.002 和 0.003 的差距只有 0.001。
- 经过 Gamma 变换后，0.079 和 0.105 的差距变成了 0.026，放大了 26 倍。
- 放大后的差距足以让它们在 ×255 取整之后，落到不同的整数上。

简单说：**Gamma 把暗部微小的差异"放大"了，让它们在被量化之前就已经足够大，不会被四舍五入吞掉。**

---

说这么多，希望能够建立一种物理直觉：RGB只是一种坐标表示，而非物体本身性质的数值表示，受到参考系和环境的直接影响。

了解这一点之后，下一步就是进行解耦，在NeRF/3DGS这个领域，从数学角度来说基于物理光照方程的计算只在线性空间成立，从物理角度来说，我们希望机器首先能够学到物体在统一光照环境下的颜色数据。所以，必须对照片(sRGB)进行逆Gamma过程，得到LinearRGB，在线性空间进行各种计算


--- 

等等有些乱，我们重新梳理：

1. 拍照：光线->像素的过程
2. 科研：图片（像素）->RGB（事实上，目前就是直接用原图片）

**先说拍照过程：光线->像素**

光线（包含不同波长 $\lambda1, \lambda2, \lambda3...$）进入镜头，经过多组透镜。

在光线打到传感器之前，会经过一层 $CFA (Color Filter Array)$，常用的CFA是 $Bayer\space CFA$ ，就是常说的 Bayer阵列  。物理上就是一层非常小的彩色滤光片阵列，类似下面的样子：
```
G R G R
B G B G
G R G R
B G B G
ps:RGB分别表示某种颜色的滤光片
```
每一个滤光单元后就是一个感光单元，经过 $BayerCFA$ 之后，光才真正的打到感光单元上。

打到感光单元之后，利用半导体的内光电效应，传感器本身直接得到的是电子数量（电荷，与光强直接相关），然后将电荷的积累转换成电压模拟信号。

下一步，由于这个电压值很可能非常小，容易被噪声直接淹没，于是，就要进行一次增益（模拟信号的增益，称为模拟增益），将电压值进行系数倍增，谨记，噪声也同时增大了。

有了电压模拟信号，就可以转换成数字信号交给计算机处理了，这步转换依靠的就是 $ADC (Analog\space Digital\space Converter)$ 。
他的作用就是将连续的电压转换成有限整数，比如说 12-bit ADC 有 $2^{12} = 4096$ 个等级，
所以，转化之后，将得到0-4095的数据阵列，这个过程是一个线性变化。得到的数据阵列我们就叫做 RAW数据 ，在这里就是 $Bayer\space RAW$ 


> 到这里，我们得到了RAW数据，这个RAW数据和真实光强近似线性（考虑到噪声）它尽可能的保留了光、电、数字三者的测量关系，此刻也正式的从模拟信号进入方便处理的数字信号，也从RAW数据开始，分成了不同的路线，也会对这个数据进行不同的处理


我们直接从这里跳到第五章
# 第二部分：从相机到多视图三维几何

## 第五章：相机模型：从空间点到像素射线

### 5.1 先把相机想象成一个”方向记录器“

首先我们忽略景深，模糊，镜头像差等等，抽出最稳定的一层光学关系：一个像素对应从相机光心出发的一个方向。这也解释了在最后的二维图像上为什么天然的缺少深度



### 5.3 四套坐标在脑海中同时存在

1. 世界坐标（ $X_w,Y_w,Z_w$ ）
	描述一个点在真实世界的什么位置。也是描述场景中点和相机共同存在的参考系
2. 相机坐标（ $X_c,Y_c,Z_c$ ）
	原点位于相机光心，Z轴指向实际物体
3. 归一化成像平面坐标 （ $x_n,y_n$）
	原点在相机前侧 $Z=1$ 处，成像平面上的点坐标
4. 像素坐标（ $u,v$ ）

四种坐标的关系如图：

![[Pasted image 20260828103228.jpg]]

像素坐标：

![[Pasted image 20260828105411.png]]

下一步就是怎么进行坐标间的转化：

$$
Pixel\space Coordinate\to Camera\space Coordinate\to World\space Coordinate
$$

### 5.4 坐标转化的基本设定

首先我们要去理解为什么需要这样的转化，这几个坐标具备怎样的基础设定

想像你站在房间中心（光心）：

- 像素坐标 = “我手指指向墙上第（320，240）块瓷砖
	取决于墙有多远（像平面Z坐标），瓷砖多大（相机决定的单位大小）。换一面墙， 数字就发生变化
- 归一化坐标 = ”我手指偏离正前方水平15°，竖直8°“
	只和你手指指向的方向有关，换任何一面墙，这个角度不变

那么如何做到这一点的呢：

我们假设像素坐标为 $(u,v)$

光心在像素坐标系坐标为 $(c_x,c_y)$ 

相机焦距（像平面）为 $F(m)$ ，然而由于像素未必为正方形，所以像素焦距的计算：

$$
\begin{align}
f_x&=\frac{F}{p_x} \\\\
f_y&=\frac{F}{p_y}
\end{align}
$$

​​其中 $p_x$ 和 $p_y$ 分别表示一个像素的长宽尺度

那么从相机光心指向像素的角度坐标就是：


$$
(x_n,y_n)=(\frac{u-c_x}{f_x},\frac{v-c_y}{f_y})
$$

为了直接得到这个坐标转换，我们引入内参矩阵：


$$
K= 
\begin{bmatrix}
f_x & 0 & c_x \\
0 & f_y & c_y \\
0 & 0 & 1
\end{bmatrix}
$$

那么两个坐标转化：

$$
K\cdot
\begin{bmatrix}
x_n \\
y_n \\
1
\end{bmatrix}=
\begin{bmatrix}
u \\
v \\
1
\end{bmatrix}
$$

好了，有了方向，下面乘深度就可以得到距离了，于是有：


$$
\begin{bmatrix}
X_c \\
Y_c \\
Z_c
\end{bmatrix}=
Z_c\cdot
\begin{bmatrix}
x_n \\
y_n \\
1
\end{bmatrix}
$$

这里就可以理解成归一化坐标的第二种定义：

$$
\begin{align}
x_n &= \frac{X_c}{Z_c} \\\\
y_n &= \frac{Y_c}{Z_c}
\end{align}
$$

也就是另一种写法：

$$
Z_c\begin{bmatrix}
u \\
v \\
1
\end{bmatrix}=K\cdot
\begin{bmatrix}
X_c \\
Y_c \\
Z_c
\end{bmatrix}
$$

最后，在相机坐标系还不够，我们要进一步进入世界坐标系。也就是相机坐标系和世界坐标系的转化，这里就要引出外参矩阵的概念

我们知道，这种转化需要解决两个问题，平移和旋转。

相机在世界坐标系的位置： $t=(t_x,t_y,t_z)$

相机在世界坐标系中的旋转状态：三维正交矩阵 $R$

像刚刚二维一样，我们引入一个四维矩阵同时完成旋转和平移的工作，这个矩阵我们就称为外参矩阵：


$$
T_{cw}=
\begin{bmatrix}
R & t \\
O & 1
\end{bmatrix}=
\begin{bmatrix}
r_{11} & r_{12} & r_{13} & t_x \\
r_{21} & r_{22} & r_{23} & t_y \\
r_{31} & r_{32} & r_{33} & t_z \\
0 & 0 & 0 & 1
\end{bmatrix}
$$

到这里，我们就有了从像素坐标到世界坐标的全过程：


$$
s
\begin{bmatrix}
u \\
v \\
1
\end{bmatrix}=
K\cdot
\begin{bmatrix}
R & t
\end{bmatrix}
\cdot
\begin{bmatrix}
X_w \\
Y_w \\
Z_w \\
1
\end{bmatrix}
$$

其中的 $s$ 是尺度因子，就相当于 $Z_c$

### 5.10 深度，欧氏距离，逆深度与视差

我们在此基础上去理解几个概念

- 投影深度 $(z{-}depth)$  ：相机坐标系中的 Z 分量
- 欧氏距离 $(range)$ ：关心到三维点的直线距离
- 逆深度 $(inverse\space depth)$ ：$1/Z$ 或某种仿射归一化形式
- 视差 $(disparity)$ ：双目对应点的位置差，整流双目中满足 $d=f\cdot \frac{B}{Z}$

## 第六章：两视图几何

### 6.1 从 $Z_c$  开始

我们知道，内参和外参矩阵都是可以事先得知的，整个过程只有 $Z_c$ 是未知的。

那么如何得知这个 $Z_c$ 呢。就进入了我们所说的两视图几何的领域

先理解一下我们之前的一些内容：

$$
P_c=Z_c\cdot K^{-1}\cdot p
$$

其中：
- $p$ 是三维像素坐标：
	$$
	p=
	\begin{bmatrix}
	u \\
	v \\
	1
	\end{bmatrix}
	$$
- $K^{-1}$ 为内参矩阵的逆
- $P_c$ 是相机坐标系的坐标：
	$$
	P_c=
	\begin{bmatrix}
	X_c \\
	Y_c \\
	Z_c
	\end{bmatrix}
	$$

可以发现，如果我们将 $K^{-1}\cdot p$ 视作方向的话，那么 $Z_c$ 就是放大倍数。所以就有了下面的两视图几何的核心思路：

在两个视角定位同一个点，然后利用两个方向确定这个点。

```

                 3D世界

                     ● P
                    / \
                   /   \
                  /     \
                 /       \
                /         \
               /           \
              /             \
             /               \
        Camera 1           Camera 2
            ●----------------●
```

我们在两个相机中间连上一条线，这条线就叫做 $Baseline$ （基线）

并且我们将三点确定的平面称作 $Epipolar\space Plane$ （极平面）

极平面和像平面的交线就是 $Epipolar\space Line$ （级线）

下一步，将基线延长，他将会和两个相机的 $Image\space Plane$ （像平面）相交 ，形成两个 $Epihole$ （极点） 


![[Pasted image 20260903113210.jpg]]

### 6.2 本质矩阵——极线几何

了解完了所有基础概念之后，下面就是我们如何确认两个点对应三维空间唯一的点。

借用上面的图，我们假定：

$$
X_1 =\begin{bmatrix}x_1 \\ y_1 \\ 1 \end{bmatrix},
X_2 =\begin{bmatrix}x_2 \\ y_2 \\ 1 \end{bmatrix},
O_1O_2 = t = \begin{bmatrix}t_1 \\ t_2 \\ t_3 \end{bmatrix}
$$

其中，$X_1$ 和 $t$ 的坐标系是 $O_1$ ，$X_2$ 的坐标系是 $O_2$ 。

下面我们都转移到 $O_1$ 坐标系中。

$$
X_{X_2\space in\space left} = R\cdot X_2 + t 
$$

那么如果是同一点，会有 $X_1$ ，$X$ ，$t$ 共面，则：
$$
\begin{align}
0 &= X_1^T\cdot (t\times X) \\
&=X_1^T\cdot [t\times (RX_2+t)] \\
&=X_1^T\cdot(t\times RX_2)
\end{align}
$$

而叉乘的矩阵表示，对于向量 $v_1$ 和 $v_2$ ,引入反对称矩阵的表示：
$$
v_1\times v_2 = [v1]_{\times}\cdot v_2,其中\space [v_1]_{\times}=
\begin{bmatrix}0 & -z_1 & y_1 \\ z_1 & 0 & -x_1 \\ -y_1 & x_1 & 0 \end{bmatrix}
$$
那么继续转化为：
$$
\begin{align}
0 &= X_1^T\cdot (t\times RX_2) \\
&= X_1^T\cdot [t]_{\times}R\cdot X_2
\end{align}
$$

此时我们就定义：
$$
E = [t]_{\times}\cdot R
$$

这里的 $E$ 我们就称为 **本质矩阵**

就可以得到著名的 **对极约束方程**
$$
X_1^TEX_2=0
$$

### 6.3 基础矩阵——融入内参

有了本质矩阵，里面直接操作的是归一化坐标，然而平常我们处理更多的是像素坐标，所以，我们融入内参矩阵进一步转化：
$$
\begin{align}
X_1 &= K_1^{-1}\cdot p_1 \\
X_2 &= K_2^{-1}\cdot p_2
\end{align}
$$

带入对极约束方程：
$$
\begin{align}
0 &= (K_1^{-1}p_1)^TE(K_2^{-1}p_2) \\
&=p_1^T\cdot K_1^{-T}EK_2^{-1}\cdot p_2
\end{align}
$$

此时我们定义：
$$
F = K_1^{-T}EK_2^{-1}
$$

这里的 $F$ 我们就定义为基础矩阵

于是对于两幅图片的同一点，我们有约束：
$$
\begin{align}
p_1^T\cdot F\cdot p_2 &= 0 \\\\
其中：F= K_1^{-T}EK_2^{-1}&,E = [t]_{\times}R \\\\
其中：(R,t)是P_2变化&到 O_1坐标系的参数
\end{align}
$$

### 6.4 从像素点中抽出基础矩阵

在我们开始计算基础矩阵之前，必须要先产生匹配点对（匹配点即两张图片中表示世界坐标系中同一点的两个点）。

#### 6.4.1 匹配点对的产生

利用各种匹配算法，我们在两张图片（左图和右图）中进行点对匹配，会得到 $N$ 对对应点，形成左右图点矩阵：
$$
P_L,P_R(N\times 3)
$$

每一行都是一个点的像素齐次坐标：
$$
P_i = (u_i,v_i,1)*N
$$

#### 6.5.2 坐标归一化，防止数值崩溃

像素坐标通常是非常大的数，直接相乘会导致几百万的数值，所以我们要对数据进行归一化。

两个点矩阵分别进行如下操作，以左矩阵为例：

1. 计算所有点的质心 $(\bar{u}_L,\bar{v}_L)$ 和所有点到质心的平均距离 $d_L$ 
2. 构造一个 $3\times 3$ 的归一化变换矩阵 $T_L$ ，这个矩阵先平移，后缩放:
	$$
	T_L=\begin{bmatrix}
	\frac{\sqrt{2}}{d_L} & 0 & -\bar{u}_L\frac{\sqrt{2}}{d_L} \\
	0 & \frac{\sqrt{2}}{d_L} & -\bar{v}_L\frac{\sqrt{2}}{d_L} \\
	0 & 0 & 1
	\end{bmatrix}
	$$

3. 进行归一化：
	$$
	\hat{P}_L = P_L\cdot T_L^T 
	$$

同理，得到 $\hat{P_R}$ 

#### 6.5.3 RANSAC 抽样得到最优基础矩阵

RANSAC全称 RANdom SAmple Consensus 中文：随机抽样一致性

在开始抽样之前，我们来看一下基本的验证思路：
找到一对匹配点，然后代入约束方程检验：

$$
x_1^TFx_2=0 
$$

展开来有：
$$
\begin{align}
0=x_1^TFx_2 =&
\begin{bmatrix}u_1 & v_1 & 1 \end{bmatrix}\cdot 
\begin{bmatrix}
f_{11} & f_{12} & f_{13} \\
f_{21} & f_{22} & f_{23} \\
f_{31} & f_{32} & f_{33}
\end{bmatrix}
\cdot \begin{bmatrix}u_2 \\ v_2 \\ 1 \end{bmatrix} \\\\
=&\begin{bmatrix}u_1 & v_1 & 1 \end{bmatrix}\cdot
\begin{bmatrix}
f_{11}u_2+f_{12}v_2+f_{13} \\
f_{21}u_2+f_{22}v_2+f_{23} \\
f_{31}u_2+f_{32}v_2+f_{33}
\end{bmatrix}\\\\
=&\space f_{11}u_1u_2+f_{12}u_1v_2+f_{13}u_1\\
&+f_{21}v_1u_2+f_{22}v_1v_2+f_{23}v_1\\
&+f_{31}u_2+f_{32}v_2+f_{33}
\end{align}
$$

也就是一对匹配点，产生一个齐次线性方程。由于是齐次，所以实际上只有八个自由度，于是我们只需要找到八个点就可以确定所有系数。

下面就是基于这种思路产生的一种算法框架RANSAC。

RANSAC是一种思路，下面的操作就是在 $P_L$ 和 $P_R$ 中随机抽取八对点。

我们将基础矩阵拆开成一维列向量：

$$
f=\begin{bmatrix}f_{11},f_{12},f_{13},f_{21},f_{22},f_{23},f_{31},f_{32},f_{33} \end{bmatrix}^T
$$

那么我们抽出了八对点，我们就可以构成系数矩阵 $(8\times 9)$ ：
$$
\begin{bmatrix}u_1u_2&u_1v_2&u_1&v_1u_2&v_1v_2&v_1&u_2&v_2&1 \end{bmatrix}
$$

就有线性方程组：
$$
A\cdot f=O
$$

对于计算机解这个方程组，一般就是SVD了。

得到一个初步的基础矩阵 $F$ ，然而这个 $F$ 虽然满足八点线性约束，但是通常是一个满秩矩阵，然而真正的基础矩阵必须是一个奇异矩阵，它的秩严格等于2。

所以，我们将这个 $F$ 再做一次SVD 分解。
$$
F = U_F\Sigma_FV_F^T
$$

并强制的令 $\Sigma_F$ 中的最小奇异值为0，得到新的奇异值矩阵 $\Sigma_F'$ ，然后重构基础矩阵：
$$
F_{final}=U_F\Sigma_F'V_F^T
$$

得到一个最终的基础矩阵。

下面对这个F进行“考试”。将所有的点对带入计算，右侧重投影误差小于我们设定的阈值，就将这个点对标记为内点，否则标记为外点。
$$
x_1^TFx_2=S
$$
所有点对测试完之后，统计当前这个基础矩阵支持了多少个内点。迭代这个过程100到1000次之后，得到一个最优的基础矩阵F，并同时得到其对应的内点集和外点集。

#### 6.5.4 从最优矩阵到相对位姿

有了$F$ 之后，我们进一步依赖内参矩阵求解本质矩阵：
$$
F=K_1^{-T}EK_2^{-1}
$$

那么就有：
$$
E=K_1^TFK_2
$$

这个时候就把本质矩阵摘出来了。

下一步就是从本质矩阵中拆出相对位姿参数 $(R,t)$ 

还记得吗，本质矩阵的定义：
$$
E=[t]_\times R
$$

如何拆解呢，这里用了一种很巧妙的SVD技术：

构造一个特殊矩阵，并对E进行SVD分解：

1. 定义辅助矩阵 $W$
$$
W=\begin{bmatrix}0&-1&0 \\1&0&0 \\0&0&1 \end{bmatrix}
$$

2. 对E进行SVD分解：

$$
E=U\Sigma V^T
$$
3. 利用U，V，W可以组合出4组可能的 $(R,t)$
	-  $R_1=UWV^T$ , $t_1=U[:,2]$ （即U的第三列）
	-  $R_2=UW^TV^T$ ,$t_2=U[:,2]$ 
	-  $R_3=UWV^T$ ，$t_3=-U[:,2]$ 
	-  $R_4=UW^TV^T$ ,$t_4=-U[:,2]$ 

4. 三角化验证
	这四组解在数学上都满足E的方程，但是物理上只有一组是正确的。
	下面就需要利用内点集，对每一组(R,t) 进行三角化，计算出三维点的深度，只有计算出来的Z>0，才是正确的解。具体的DLT（$Direct\ Linear\ Transformation$ ）方法在此省略（直接线性变换）

### 6.5 构建初始地图和确定世界坐标系

目前为止，我们只处理了两张照片，得到了一部分有深度的内点，世界坐标系是我们的第一个相机，我们称这些点为地图点，并通过这些点初始化了世界地图，系统下面就会进入循环跟踪模式：

当第三张照片进入时，我们就不能使用2D－2D的对极几何处理了，取而代之的是利用已经建立好的初始三维地图点，通过PnP算法求解第三帧的位姿，随后是第四张，第五张…不断循环“$匹配\to 求解位姿\to 拓展地图\to 优化$ ”的过程

到这里，2D－2D对极几何正式结束，接下来就是3D－2D的PnP问题

## 第七章：从二维到三维

### 7.1 第三张照片——PnP

PnP $(Perspective-n-Point)$ ，意味着我们不再依赖两张照片去猜测（或者计算）两个相机的相对运动，接下来我们将直接利用已经建好的三维地图，直接锁定新相机的位置。

当第三张照片到来时，完整的求解过程有以下四个关键步骤：

1. 特征匹配和3D－2D关联
2. PnP求解相机位姿
3. 非线性优化，光束法平差BA
4. 扩展地图

### 7.2 第一步，特征匹配，降维关联

这一步简单来说就是将新图像上的新二维像素点和已经建好的三维地图点进行精准连线

我们通常采用经典的ORB特征提取和匹配策略

ORB $(Oriented\ FAST\ and\ Rotated\ BRIEF)$ ，整个过程分为三个步骤：
- 关键点检测（FAST）：ORB算法首先利用FAST角点检测器，在图像中迅速找到那些亮度变化剧烈的像素点（即角点）