为了让你彻底看懂，我们直接来看一封真实的“HTTP 请求数据”长什么样。当你在浏览器点击一个链接时，浏览器在后台生成的原始文本通常是这样的：

```
GET /index.html HTTP/1.1
Host: www.baidu.com
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)
Accept: text/html
Cookie: session_id=abc123456

```

别看它简单，这短短几行文本包含了极其丰富的信息。我们可以把它拆解开来看：

### 1. 请求行（第一行）：我要干什么？

`GET /index.html HTTP/1.1`

- **GET**：这是“动作”。告诉服务器，我只是想“获取/读取”一个文件，而不是提交表单（POST）或修改数据。
- **/index.html**：这是“目标”。告诉服务器，我要的是你根目录下的 `index.html` 这个网页文件。
- **HTTP/1.1**：这是“语言版本”。告诉服务器，我使用的是 1.1 版本的 HTTP 协议，请你用同样的版本回复我。

### 2. 请求头（Header）：我的身份和具体要求

接下来的每一行都是“键: 值”的格式，用来提供附加信息：

- **Host: [http://www.baidu.com](http://www.baidu.com/)**：告诉服务器你要访问的具体域名（因为一台服务器上可能托管了成千上万个网站）。
- **User-Agent**：相当于你的“名片”。告诉服务器“我是一个运行在 Windows 10 上的 Edge 浏览器”。（很多网站就是通过看这个字段，来决定给你展示手机版还是电脑版网页的）。
- **Accept: text/html**：告诉服务器“我能看懂网页格式的数据”。
- **Cookie**：相当于你的“通行证”。服务器通过它认出你，比如知道你已经登录了账号。

### 3. 空行（极其关键！）

注意上面代码的最后一行是一个**空行**。这在 HTTP 协议中是强制要求的。它就像是一个“分割线”，告诉服务器：“我的要求（请求头）说完了，如果下面还有内容（比如你上传的附件或表单数据），请从这里开始读。”

---
# 总结

HTTP 请求数据”就是**浏览器按照 HTTP 协议的语法规范，在内存中生成的一段包含“动作、目标、身份要求”的纯文本字符串**。它是应用层处理的核心产物，生成完毕后，它就会被“向下传递”给传输层，准备套上 TCP 的包装。

返回：[[计算机网络初识 —— ”一次意外的拒绝“#^feb498]]