# 一、*物理网卡* 

硬件层，电脑上真实存在的硬件，比如 **插在主板上的网线接口** 或者是 **笔记本中内置的接收 Wi-Fi 信号的无线网卡芯片** 。这是数据包通向真实世界的唯一物理出口
# 二、 *虚拟网卡* 

网络层，不关心是从哪个软件发出的数据，只要操作系统准备将数据包发往外部网络，虚拟网卡就会强行拦截这些数据包

`ps` : 系统代理是软件层，针对书写的规则，拦截其中 

返回：[[计算机网络初识 —— ”一次意外的拒绝“#^e6b66d]]