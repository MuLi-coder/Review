# TODO-1

1. 什么是三维前馈重建网络
2. 什么是三维重建和三维高斯泼溅模型
3. 单位区别究竟会导致什么样的问题

# 梳理

## 1. 课题的产生

### 1> 一个有意思的思想

回看表情生成的项目时，我们尝试提炼位置编码背后的抽象逻辑，他是怎样做的？

有一个关键的概念：

**坐标系的自适应**

这种自适应体现在中心化和对于尺度和偏移的学习上。

那么这种自适应的想法，可否迁移到多视图三维重建上呢？**模型也许可以自己决定应当用什么样的坐标规范**

### 2> 要不加一个学习模块？ 

考虑到实际输入参数的复杂性，我们想到能不能在进入实际处理数据的网络之前，添加一个模块来进行数据的规范化。

并且注意到这样的操作，理论上是不受我们的数据单位（物理量纲）的影响，其背后的思想也就是：

**数字可以变化，但是对物理世界的理解应当重合**


这个应当是正确的吧。

>在我们的物理学中，统一的量纲在公式的作用下，同样具备运算性质。
>
> 其实，鲜有同学注意到一件过渡。初中的答题格式总是要求保留单位，乃至高一都是如此。然而一切的转折发生在 *国际单位制* 这一节课程之后，学生被允许只进行数字运算，只需要计算的数字单位满足统一单位制的规范即可，其背后就是物理的量纲运算性质在支持。这也无形中在我们的脑海中刻下了**单位统一即可保持物理规律的不变性**这样的默认规律

欸？这个观点在神经网络中真的成立吗？

把这个疑问落实到这个课题中：

这个学习的模块，到底是不是必须额外增加的。

**还是说会像物理公式一样，单位的问题在计算中被自然的抹去了** 

牢记这句怀疑，他是后文探索课题方向的核心思路。


### 3> 关键的转折：否决实验

为此，我们设立了一个否决实验：

**如果非常简单的确定性坐标规范化(比如减去相机中心、再除以相机分布的均方根尺度)，就已经把问题解决了，那么就不应该再把一个可学习模块包装成主要贡献。**

转折出现了，这个否决条件被触发了。

这个模块本身不再具备研究价值。但是我们关于那句怀疑依然没有答案，不如就简单的测试一下吧。

### 4> 惊人的发现

最近大放异彩的前馈三维重建网络大放异彩。

> 输入图片，相机（内参（外参）），基于transformer的前馈神经网络就可以给出深度，外参，点云，或者高斯泼溅的参数等等，用于后续的渲染工作

我们发现部分训练好的模型在单位放大或者缩小后的表现不尽人意。

课题的目标似乎越来越清晰：

**单位变换，答案随之更改**

我们的质疑在这一刻轰然坠地，我们习惯了许久的默认规则在这里却并不适用。

我们习惯性的会对不同的变量赋予对应的 ”长度指数“ k: 最朴素的长度（k=1），面积（k=2），体积（k=3)等等，与之相关的概念的是单位等变性，理论上而言，如果先将所有输入转化为无量纲（k=0），那么机器学习的结果将永远服从物理规律

那么自然萌生的问题就是，网络的什么地方打破了物理近乎严苛的量纲规律？

### 5>三个问题，三支蜡烛

#### 1- 第一个问题，不同量纲的加法运算

主要体现在：

**精细逆深度**(k=-1） = **粗略逆深度**(k=-1) + **神经网络残差**(k=0)

> 关于非常见变量的k，我们可以通过变化来观测，类似于导数

扩大化这个问题，表明：

**计算的链路中可能存在不止一处的量纲运算问题**

#### 2- 第二个问题，问题的温床无处不在

主要体现在高斯尺寸的修正，让问题本身不只是属于相机位置编码。
这其实表明：

**在整个工程链路中，随处都有可能产生这样的问题。**

#### 3- 第三个问题，数值不只有物理量

关于那个0.001，保护项在这里同样造成了影响。

这表明：

**并非只有物理量需要考虑，每一个写死的数值都不能忽视**

---

三个问题，三支蜡烛，课题渐渐清晰

### 6> 想法到行动：具体的工程任务

#### 1- 测试

我们希望通过人为的输入不同倍率的数据表示，在同一个冻结模型上进行测试。

我们比较产出：

深度，高斯中心，协方差，乃至渲染图像

是否一致

#### 2- 定位

如果运行出现错误，我们希望知道到底是什么地方导致了错误

在过程中不断取出中间张量，然后测试其 K 值（长度指数），对比其语义本该具有的 K 值。

如果产生参差，将此处标记可疑 ”量纲断点“

好消息是，前研究排除了投影几何前缀，已经把问题的范围缩小到神经网络的细化接口处开始

在不断的搜索中，找到可以断点的最前端。

> 这有点像 assert断言：我们不断将断言的位置提前，找到断言通过与未通过的交界处

> 额外的想法，这样的敏感神经层是否具有可推广性，也就是：是该模型使得该层成为敏感层，还是敏感层本身就有其敏感的性质
#### 3- 修复

修复任务的核心要求：

**在老模型原本的训练量纲规范下， 必须等同于什么都没做**

基于我们在前两步中找到的敏感神经层，我们在此前加入某种”单位转换插头“：

**无论传到这里是什么样的单位，在进入第一个敏感层之前，先转化成熟悉的数字单位。从最后一个敏感层输出后，修正为调用者使用的单位**

> 这有点像数据包end-to-end传播的过程，单位转换插头就是 调制解调器，光缆即是神经网络

##  二、课题的最后梳理

最后用文字重新说明这次的工作：

**前馈三维重建网络的参数输入时，单位的不同，会改变其对场景的理解，导致最后高斯泼溅的渲染结果不尽人意。我们希望系统的定位这些量纲泄露的位置，测试其量纲后，用一种较小影响因子的方式修复他**


---


# TODO-2

1. transformer 的具体内容以及其在前馈网络中的融合和应用
2. 四种前馈网络模型的应用区别
3. 目前已经完成的实验，具体做了什么，遗憾在什么地方
4. 未来将要做什么，“扩大规模” 的具体内涵

现在的任务是继续gua