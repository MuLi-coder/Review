# 一、引言

虽然之前自诩有学习 `web` 开发，并确实得到了一些成果。

在此过程中，我也有认真复盘（参见 [[Django_review]] ），在这个过程中我也确实摸清楚了基本的网络基本模式，但是只是简单的理解：来自客户端和服务端的反复数据请求（ `GET` 和 `POST` ）数据在本地和服务器之间的传递过程。

但是，这一次，一次 "意外的拒绝"，却让我理解了网络的另一个部分：浏览器，搜索引擎， `url` 访问，`vpn`, 节点，代理 等等。

虽然一个下午的探索遇到了很多本不该出现的问题，甚至这整个事件的起因都很搞笑，不过也成了激发我进一步认识的动力。

# 二、”意外的拒绝“

今天，你像往常一样打开电脑，挂上梯子（ `clash verge` ），然后像往常一样打开了 `Edge` ，似乎一切正常，毕竟，收藏夹里的千问等等网址依旧存在，并且可以正常的打开。

但是这时候，搜索框中输入简单的“百度”，却转了半天，很奇怪，以为是节点坏了，于是到 **超级加速** 中想去看看剩余的流量。然而，收藏夹中的网址居然打不开了。

两次奇怪的请求拒绝，拉开了今天探索的序幕。至于为什么叫意外，往下看，你会知道的。

# 三、必应把我屏蔽了？

“必应把我的 `ip` 屏蔽了？” 看到 AI 的回答，倒惊呀了一瞬。这很奇怪，仔细回想，之前似乎也出现过这个情况，一模一样，但当时好像瞬间解决了，关闭代理，好像就好了，对！关闭代理就可以了。于是，我就把 `clash verge` 叉掉了（这也是一处伏笔），结果再次打开超级导航 ，依旧不行，一问之下，又在搜索框中输了各种数字，依旧是 `Bing 拒绝了连接` 。

重启。

等待了一小会，重新开机了，直接打开浏览器，随便搜索一番，结果令人高兴：果然没有重启解决不了的问题。

暂时不管了，挂梯子。

结果，坏了，又不行了。捣鼓半天，依旧不行吗。难道又要重启了？不过仔细一想，区别在哪，刚开机的时候没有一点问题，但是打开代理之后就不行了，哪怕把窗口关闭。

所以很有可能是代理的问题，可是我已经关闭窗口了欸（有人说，关掉就是关闭代理）

坏了，不会真被 `bing` 屏蔽了吧。继续看AI的说法：有的IP会被微软加入黑名单。那很坏了，我在家都能被加入黑名单，好像还是连着我的节点一起加入黑名单了。

然后，我先猜测说有没有可能是梯子脏了。于是AI让我做一个关闭系统代理的操作，再次打开，真的是有系统代理的选项 ，关闭之后，结果就可以了。

所以，其实是问题出在是否开启系统代理上。
并且，到这里突然发现了第一个大的问题，就是关闭窗口，并不会关闭系统代理，只有手动关闭之后才算（超级搞笑来的）

可是，到底出现了什么问题，难道真的是代理节点被 `Bing` 屏蔽了吗 ？

# 四、一系列的实验

细细一想：“好像不对，也不完全是被 `bing` 屏蔽了，应该改成只是不能搜索，因为收藏夹里的好像都没问题。“

那么到底是谁的问题呢，我简单梳理了一下目前已有的线索：

| 编号  |    操作    | 是否走系统代理 |     结果     | 速度  |
| :-: | :------: | :-----: | :--------: | :-: |
|  1  | 收藏夹其他网址  |    是    |     正常     |  快  |
|  2  | 收藏夹超级导航  |    是    |     失败     |  -  |
|  3  | 收藏夹其他网址  |    否    |     正常     |  快  |
|  4  | 收藏夹超级导航  |    否    |     正常     |  快  |
|  5  |  直接搜索数字  |    是    |     失败     |  -  |
|  6  |  直接搜索数字  |    否    |     正常     |  快  |
|  7  | 直接搜索国内网址 |    是    |     正常     |  快  |
|  8  | 直接搜索国内网址 |    否    |     正常     |  快  |
|  9  | 直接搜索国外网址 |    是    | 可以进但几乎就是失败 | 极慢  |
| 10  | 直接搜索国外网址 |    否    |     失败     |  -  |

我们俩梳理几个规律：

1. 不走系统代理：一切正常：国内正常访问，国外访问不了（3，4，6，8，10）
2. 走系统代理：
	1. 收藏夹除超级导航外的网址，都正常（1）
	2. 直接搜索国内或者国外的网址，都正常，但是国外网址极慢，相当于失败（7，9）
	3. 走代理不能打开超级导航（2）
	4. 走代理不能搜索除网址外的东西（5）

那么我们可以稍微推测一下可能的原因：

1. 首先，不走系统代理一切正常，所以问题来源是走代理路线的过程出现了问题
2. 其次，走代理的时候，直接搜索网页都是正常的，只有搜索别的东西的时候会出现问题，所以，搜索网页和搜索其他内容是有区别的

注意这个 2 ，搜索网页和搜索其他内容是有区别的，我是在浏览器上方的框中直接输入的，后来也有在下面的大框中尝试，于是我在打开系统代理的情况下做了几个实验，结果如下：

| 编号  | 搜索位置 | 搜索内容 | 结果  |
| :-: | :--: | :--: | :-: |
| 11  | 地址框  | 其他内容 | 失败  |
| 12  | 地址框  |  网址  | 正常  |
| 13  | 搜索框  | 其他内容 | 失败  |
| 14  | 搜索框  |  网址  | 失败  |

经查，原因如下，也是本篇 **第一个知识** ：

- 地址框：早些时候只能搜索网址，现在网址和一般内容都可以搜索
- 搜索框：搜索的东西都被当做查找的内容，又搜索引擎进行查找

搜索引擎到底是什么，以前经常听说，于是本篇 **第二个知识** ，关于 浏览器 和 搜索引擎 ：

- 浏览器：负责直接访问目标网址服务器，和渲染网页
- 搜索引擎：负责对输入的内容,利用特定的算法，将相关的内容，返回交由浏览器呈现

所以，二者到底是怎么工作的：

- 在地址框输入网址，被浏览器包装好，和服务器进行交互，传回数据，渲染呈现

- 在搜索栏搜索内容，并被浏览器打包标准的HTTP GET请求( `https://www.baidu.com/search?···` )，传到搜索引擎所在服务器进行计算，然后返回相应的内容

- 现代，在地址栏搜索内容，被识别为不是网址，会进行和上一种情况一样的操作

所以简单来说，本质： **浏览器的工作就是和服务器进行交互，并且接收返回的数据进行渲染。**

常见的浏览器和其默认配套的搜索引擎：

- `Edge` 和 `Bing` ; 
- `Chrome` 和 `Google` ;
- 百度浏览器（移动端）和 百度；
- 360浏览器 和 360搜索；

那么我们就能进一步对上面的内容进行分析了：

我们发现，只要是设计到 **开启系统代理代理 和 Bing** 或者 **开启系统代理 和 国外网站** 这两种情况，都会报废，或者说访问极慢近乎报废

这个时候我们就要了解一下系统代理的工作原理了，本篇 **第三个知识点** ：

- **第一步：底层立规矩**

修改操作系统的底层网络配置，所有网络请求在送给运营商之前，要先到统一的端口进行处理。

当你打开 `Edge` 并发出网络请求的时候，会先问一下操作系统：“老大，我要出门，走哪一个通道？”操作系统就会回答，并给出相应的结果。

这个时候，所有的流量将从同一个地方发出。

- **第二步：分配路径**

数据进入端口，进行检测：

1. 国内正常网址：
		直接选择 `DIRECT` ,数据包直接从局域网 `IP` 发出，进入运营商
2. 国内被墙的网址：
		进入代理模式 `Proxy` ,数据包进行再次加密，伪装成正常出国请求，发往代理节点

- **第三步：结果回传**

数据包从服务器原路返回，代理数据可以正常通过防火墙。浏览器接收数据，进行前端渲染。

# 五、总结问题

那么我们再次回看第一个表格：

| 编号  |    操作    | 是否走系统代理 |     结果     | 速度  |
| :-: | :------: | :-----: | :--------: | :-: |
|  1  | 收藏夹其他网址  |    是    |     正常     |  快  |
|  2  | 收藏夹超级导航  |    是    |     失败     |  -  |
|  3  | 收藏夹其他网址  |    否    |     正常     |  快  |
|  4  | 收藏夹超级导航  |    否    |     正常     |  快  |
|  5  |  直接搜索数字  |    是    |     失败     |  -  |
|  6  |  直接搜索数字  |    否    |     正常     |  快  |
|  7  | 直接搜索国内网址 |    是    |     正常     |  快  |
|  8  | 直接搜索国内网址 |    否    |     正常     |  快  |
|  9  | 直接搜索国外网址 |    是    | 可以进但几乎就是失败 | 极慢  |
| 10  | 直接搜索国外网址 |    否    |     失败     |  -  |
关于关闭代理的情况，所有数据都会大摇大摆的直接发出，进入网关

我们只考虑开启代理的情况：

收藏夹其他网址，均为服务器在国内的网址，所以进入代理之后，直接走 `DIRECT` 路线，并不会涉及海外节点，所以正常访问。

直接搜索数字或者其他内容，因为浏览器使用 `Edge` ，并且是搜索非网址的内容，所以，浏览器发出的请求将送到 `Bing` 服务器，虽然 `Bing` 在国内有相应的服务器，但是可能是梯子脚本的问题，判定为海外网址，于是走了 `Proxy` 路线，所以最后非常慢。

也就是，问题的产生，都是由于走了海外节点，所以问题就定格在节点上。

# 六、最后的真相

经过不懈的努力，我们将问题最终定格在了海外节点上，顺手一查，发现是订阅到期了，虽然已经重新开通，但是新的订阅并没有导入，所以只要涉及到 代理 途径，统统被限速了。

导入新的订阅之后，一切正常。

# 七、留下的问题

虽然一切似乎都已经解释完毕了，但仔细观察，其中存在许多逻辑漏洞，这套推理的逻辑并不能解释后来的情形，问题如下：

### 1. 关于 *超级导航* 和 *`Bing`*

我们回到超级导航这个特定的网址：

| 编号  | 是否开启代理 | 节点是否失效 | 结果  |
| :-: | :----: | :----: | :-: |
|  1  |   否    |   是    | 成功  |
|  2  |   是    |   是    | 失败  |
|  3  |   否    |   否    | 成功  |
|  4  |   是    |   否    | 成功  |

简单对比可以发现，再不开启代理的情况下，网址正常打开，说明他完全是一个走直连没有问题的网址。

当打开代理的时候，按照上面理论，他应该走 `DIRECT` 路线，根本不经过海外节点，理论上依旧可以正常打开。

然而，节点失效时候打开失败，节点正常时打开成功，说明这个网址走了代理路径，这如何解释？




# 八、系统重构

## 1. 数据包出发

当数据打包好之后，目标 IP 确定，准备出发前往互联网的世界。

**这个时候，是否开启系统代理，将会开启两种完全不同的路径**

## 2. 不开启系统代理的标准路径

### <1> 直接出门

数据包直接走出电脑 [[网卡]]，传到家中的路由器，然后顺着网线到了运营商哪里 ^e6b66d




   



# 拓展名词：

## *OSI 七层模型*
### 1. 层级划分

| 层级    | 做什么                       | 备注                                   |
| ----- | ------------------------- | ------------------------------------ |
| 7 应用层 | 直接面向用户                    | 常见协议：HTTP / HTTPS、gRPC、DNS           |
| 6 表示层 | 数据格式转换，加密 / 解密，压缩         | 关键点：TLS / SSL、JSON编码 / XML编码         |
| 5 会话层 | 建立 / 维护 / 断开会话            | 该层功能在现代网络中已被弱化                       |
| 4 传输层 | 端口通信，可靠传输（TCP）/不可靠传输（UDP） | 关键协议：TCP、UDP                         |
| 3 网络层 | IP寻址，路由转发                 | 关键协议：IP、ICMP（ping）；常见设备：路由器          |
| 2 链路层 | 帧（Frame）传输，MAC地址通信，局域网内通信 | 关键点：ARP协议（IP->MAC）、Ethernet；常见设备：交换机 |
| 1 物理层 | 比特流传输（0，1），电和光信号          | 常见设备：网线、光纤、集线器                       |

### 2. 直观关键词表格

|  层级   |    关键词     |
| :---: | :--------: |
| 7 应用层 | HTTP / API |
| 6 表示层 |  加密 / 编码   |
| 5 会话层 |     会话     |
| 4 传输层 |  TCP / 端口  |
| 3 网络层 |  IP / 路由   |
| 2 链路层 |    MAC     |
| 1 物理层 |    电信号     |
## *TCP / IP 模型*

如果说 OSI 七层模型是 “理论蓝图” ，那么 TCP/IP 模型则是真正的 “施工图纸” 。

这个模型一般分为四层：

|  层级   |                       管理内容                        |            核心协议             |   对应OSI层级   |
| :---: | :-----------------------------------------------: | :-------------------------: | :---------: |
| 4 应用层 |   包含所有面向用户的的应用程序协议，它不仅处理应用逻辑，还把OSI中表示和会话层合并到这一层   | HTTP/HTTPS,FTP，SMTP，DNS，SSH | 会话层+表示层+应用层 |
| 3 传输层 |   负责提供端到端的通信服务。解决网际层不保证送达的问题，根据应用的需求提供可靠或不可靠的传输   |           TCP，UDP           |     传输层     |
| 2 网际层 | 绝对核心，负责将数据包从源主机跨越多个网络发送到目的主机（路由选择）。提供尽力而为，不保证一定送达 |        IP，ICMP，IGMP         |     网络层     |
| 1 接口层 |      负责与物理硬件交互，将 IP 数据包封装成能在特定物理网络中传输的**帧**       | Ethernet(以太网)，Wi-Fi，ARP，MAC |   物理层+链路层   |

我们就以浏览器中点击一条链接🔗开始：

### 第 1 步：应用层（在电脑内部处理）

- **动作**：你点击链接，浏览器（Edge/Chrome）在内存中生成了一段 [[HTTP请求]]  ^feb498
- **内部处理**：浏览器检查这段数据，发现你要访问的是 `https://` 开头的网址。于是，浏览器在电脑内部调用加密库（如 OpenSSL），把这段明文数据加密成密文。
- **状态**：此时数据还在浏览器的进程内存里，**完全没有往外传**。

### 第 2 步：传输层（在电脑内部处理）

- **动作**：浏览器把加密好的数据，通过操作系统的 API（比如 Windows 的 Winsock）“向下传递”给操作系统的传输层。
- **内部处理**：操作系统的传输层拿到数据后，在内存中给它加上 TCP 头部（写上[[源端口和目标端口]]）。同时，它会在内部维护一个“连接状态表”，记录“54321端口正在和443端口通信”。 ^e83021
- **状态**：数据现在存在于**操作系统的内核内存**中，**依然没有往外传**。

### 第 3 步：网际层（在电脑内部处理）

- **动作**：传输层把加了 TCP 头的数据，继续“向下传递”给网际层。
- **内部处理**：网际层在内存中给数据加上 IP 头部（写上源 IP 和目标 IP）。然后，它去查阅电脑内部的**路由表（Routing Table）**。
    - _关键点_：路由表发现目标 IP `110.242.68.4` 不在本地局域网，于是路由表指示：“把这个数据包交给默认网关（你家路由器）”。
- **状态**：数据还在**操作系统的内核内存**中，**依然没有往外传**。

### 第 4 步：接口层（在电脑内部处理 + 准备往外传）

- **动作**：网际层把数据包“向下传递”给接口层（网卡驱动程序）。
- **内部处理**：网卡驱动程序在内存中给数据包加上 MAC 头部。然后，它通过 ARP 协议查出你家路由器的 MAC 地址，填在头部。
- **状态**：此时，一个完整的“以太网帧”已经在内存中组装完毕。

### 第 5 步：物理网卡（真正往外传的时刻！）

- **动作**：网卡驱动程序把内存中组装好的这个“帧”，通过主板上的总线（如 PCIe 总线），直接**写入（DMA 传输）到物理网卡的硬件缓存中**。
- **往外传**：物理网卡的硬件芯片接管了这些数据，把它们转换成电信号（网线）或无线电波（Wi-Fi），**真正地发射出你的电脑**。

